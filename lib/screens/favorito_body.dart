import 'package:flutter/material.dart';

class FavoritoBody extends StatelessWidget {
  const FavoritoBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: const Center(
        child: Text('Favoritos'),
      ),
    );
  }
}
